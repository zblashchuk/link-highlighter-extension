{
  "name": "link-highlighter-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pw:install": "playwright install chromium",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
